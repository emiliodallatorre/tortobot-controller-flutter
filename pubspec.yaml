name: tortobot_controller_flutter
description: A new Flutter application.


publish_to: 'none' # Remove this line if you wish to publish to pub.dev


version: 0.0.1+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Logica
  provider: ^4.3.2+1

  # Bluetooth
  flutter_bluetooth_serial: ^0.2.2

  # Grafica
  control_pad: ^1.1.1+1


dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
